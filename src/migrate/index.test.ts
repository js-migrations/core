/*
No migrations.
Errors on first migration (error in first of one migration).
Errors on middle migration (error in second of three migrations).
Process unprocessed migration. Unprocessed (U).
Do not reprocess migrations. Processed (P).
Skip processed migrations after unprocessed migrations. UP.
Skip processed migratons before unprocessed migrations. PU.
*/
